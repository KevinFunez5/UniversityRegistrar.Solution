@{
  Layout = "_Layout";
}

@using University.Models;

<h1>Departments</h1>

  @if (@Model.Count == 0)
  {
    <h3>No Departments have been added yet!</h3>
  }

  <ul>
    @foreach (Department department in Model)
    {
      <li>@Html.ActionLink($"{department.DepartmentName}", "Details", new { id = department.DepartmentId })</li>
    }
  </ul>

  <p class="btn btn-dark">@Html.ActionLink("Add New Department", "Create")</p>
  <p class="btn btn-dark">@Html.ActionLink("Home", "Index", "Home")</p>
