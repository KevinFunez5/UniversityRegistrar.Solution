@{
  Layout = "_Layout";
}

@model University.Models.Department

<h3>Add a New Department</h3>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.DepartmentName)
  @Html.TextBoxFor(model => model.DepartmentName, new { @class = "form-control", @required = "required" })

  <input type="submit" value="Add New Department" class="btn btn-dark"/>
}

  <p class="btn btn-dark">@Html.ActionLink("Show all Departments", "Index")</p>